/**
 * Module-level constants shared across the library.
 * Centralised here so every reference stays in sync automatically.
 */

/** Stable SVG element ID referenced by `url(#...)` in Path fill. */
export const GRADIENT_DEF_ID = 'squircleGradient' as const
